# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*~

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# go.*.sum should not be committed, but if want to check the version, commit it
#go.sum
#go.work.sum

# CDK asset staging directory
.cdk.staging
cdk.out

# python venv
venv
.venv

# nodejs modules
node_modules
#package-lock.json